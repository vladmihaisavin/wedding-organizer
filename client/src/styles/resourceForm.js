export default (theme) => ({
  paper: {
    textAlign: 'center',
    maxWidth: 936,
    margin: 'auto',
    overflow: 'hidden'
  },
  grid: {
    maxWidth: 900,
    margin: 'auto',
    overflow: 'hidden',
  },
  actions: {
    marginTop: theme.spacing(1),
    marginBottom: theme.spacing(1)
  },
  preloaderWrapper: {
    margin: '40px 16px',
  },
})